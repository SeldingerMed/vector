# Generated by `surgeval wrap`. Regenerate rather than hand-patch the pins:
# the wrapped world is Isaac-Lift-Needle-PSM-v0 at 6e47534f7d412e4be523116f250c992a63146883.
format_version = "2"
id = "orbit-needle-lift"
task_version = "0"

[metadata]
title = "Wrapped world: Isaac-Lift-Needle-PSM-v0"
modality = "robotic-kinematics"
tags = ["wrap", "isaac-lab", "metrics-only"]
safety_critical = false

[subject]
kind = "policy"

[phi]
class = "procedural"

[environment]
kind = "isaac-lab"
gym_id = "Isaac-Lift-Needle-PSM-v0"
world_pin = "6e47534f7d412e4be523116f250c992a63146883"
adapter = "or_audit.eval.sim.isaac_bridge:make_isaac_bridge"
adapter_digest = "e2727f00a209ab8b64e3080595e587e50f6a97d6e8ccd09ffd18807c80ca2f84"
parameters = {  }
n_eval_episodes = 3
seed_policy = "deterministic-eval-3"
metrics_only = true
synthetic_stub = true

# Declared so this package stays loadable where the adapter is absent;
# cross-checked against the installed adapter at load time. determinism_class
# stays `unmeasured` until `surgeval conformance` measures a seeded rerun.
[environment.capabilities]
physics = true
closed_loop = true
counterfactual = false
requires_gym_id = false
requires_world_pin = true
requires_contract = false
determinism_class = "unmeasured"

[interface]
id = "psm-cartesian-control"
interaction_mode = "closed-loop"
protocol_version = "1"
observations = ["orbit-needle-lift-obs"]
actions = ["orbit-needle-lift-action"]

[harness]
interaction_mode = "closed-loop"
protocol_version = "1"
max_steps = 60

[agent]
kinds = ["policy", "random"]
action_space = "orbit-needle-lift-action"
timeout_sec = 60.0

[oracle]
kind = "physics"

[verifier]
# A freshly wrapped world is not yet known to report every mapped signal, and
# an abstained gate is the honest outcome when it does not.
abstain_ok = true
headline = "raw_success"
entrypoint = "verifier.py:load_verifier"

[[verifier.metrics]]
id = "raw_success"
kind = "boolean"
direction = "maximize"
source = "info.raw_success"

[[verifier.metrics]]
id = "diverged"
kind = "boolean"
direction = "minimize"
source = "info.diverged"
